name: Build Packages

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build-linux:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install Yarn
      run: yarn install
    - name: Build For Linux
      id: build-linux
      run: |
        yarn build:bundle
        yarn build:linux --publish never
    - name: Pack Build Files
      run: |
        mkdir ~/Tandem-linux-AppImage
        mv dist/*.AppImage ~/Tandem-linux-AppImage
        mkdir ~/Tandem-linux
        mv dist/*.tar.gz ~/Tandem-linux
    - name: Publish AppImage
      uses: actions/upload-artifact@v3
      with:
        path: ~/Tandem-linux-AppImage
        name: Tandem-linux-AppImage
    - name: Publish Tarfile
      uses: actions/upload-artifact@v3
      with:
        path: ~/Tandem-linux/
        name: Tandem-linux
  build-windows:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Enable Developer Command Prompt
      uses: ilammy/msvc-dev-cmd@v1.12.1
    - name: Install Yarn
      run: yarn install
    - name: Build For Windows
      id: build-windows
      run: |
        yarn build:bundle
        yarn build:windows --publish never
    - name: Pack Build Files
      run: |
        mkdir D:\a\desktop\Tandem-windows
        mv D:\a\desktop\desktop\dist\win-unpacked\* D:\a\desktop\Tandem-windows
        mkdir D:\a\desktop\Tandem-windows-Setup
        mv D:\a\desktop\desktop\dist\*.exe D:\a\desktop\Tandem-windows-Setup.exe
        mkdir D:\a\desktop\Tandem-windows-appx
        mv D:\a\desktop\desktop\dist\*.appx D:\a\desktop\Tandem-windows-appx
    - name: Publish Zipfile
      uses: actions/upload-artifact@v3
      with:
        path: D:\a\desktop\Tandem-windows
        name: Tandem-windows
    - name: Publish Installer
      uses: actions/upload-artifact@v3
      with:
        path: D:\a\desktop\Tandem-windows-Setup.exe
        name: Tandem-windows-Setup
    - name: Publish Appx
      uses: actions/upload-artifact@v3
      with:
        path: D:\a\desktop\Tandem-windows-appx
        name: Tandem-windows-appx
  build-macos:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install Yarn
      run: yarn install
    - name: Build For macOS
      run: |
        yarn build:bundle
        yarn build:mac --publish never
        mv dist/*-universal.dmg ~/Tandem-macOS-universal.dmg
    - name: Publish Build Files
      uses: actions/upload-artifact@v3
      with:
        path: ~/Tandem-macOS-universal.dmg
        name: Tandem-macOS-universal
